fn main() {
    // Sequencer/prover CLI grows here: tree (M2), keys (M3), batch/prove (M4),
    // submit/measure (M6).
    eprintln!("harness: no subcommands yet (spike M0 scaffold)");
    std::process::exit(2);
}
